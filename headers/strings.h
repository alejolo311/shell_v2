#ifndef H_STRINGS
#define H_STRINGS

#include "libraries.h"

int _strlen(char *s);
char *_strdup(char *str);
int _strcmp(char *s1, char *s2);
int _strncmp(char *s1, char *s2, int n);


#endif /* H_STRINGS */
